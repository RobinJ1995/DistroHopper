<?xml version="1.0" encoding="utf-8"?>

<!-- Applied to the closing app's window. A launcher cannot animate another app's window
     towards its icon (that requires system-only RemoteAnimation APIs), so shrinking
     towards the centre while fading is the closest approximation of a contraction. -->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:interpolator="@android:interpolator/accelerate_quad">
    <scale
        android:toXScale="0.5"
        android:toYScale="0.5"
        android:fromXScale="1.0"
        android:fromYScale="1.0"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="@android:integer/config_shortAnimTime" />
    <alpha
        android:fromAlpha="1.0"
        android:toAlpha="0.0"
        android:duration="@android:integer/config_shortAnimTime" />
</set>

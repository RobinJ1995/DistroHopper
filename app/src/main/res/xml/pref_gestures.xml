<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

	<!-- Not configurable: a non-selectable hint that surfaces the fixed
	     swipe-left/right behaviour. -->
	<Preference
		android:enabled="false"
		android:selectable="false"
		android:persistent="false"
		android:title="@string/option_gesture_swipe_sides"
		android:summary="@string/hint_gesture_swipe_sides" />

	<!-- Entries/values are populated in code so the notification-tray option can
	     be hidden while the accessibility service is off. -->
	<ListPreference
		android:key="gesture_swipe_up"
		android:title="@string/option_gesture_swipe_up"
		android:dialogTitle="@string/option_gesture_swipe_up"
		android:defaultValue="open_dash" />

	<ListPreference
		android:key="gesture_swipe_down"
		android:title="@string/option_gesture_swipe_down"
		android:dialogTitle="@string/option_gesture_swipe_down"
		android:defaultValue="none" />

	<!-- Shown only while the accessibility service is disabled; launches the
	     enable wizard. -->
	<Preference
		android:key="dummy_enable_notification_gestures"
		android:persistent="false"
		android:title="@string/option_enable_notification_gestures"
		android:summary="@string/hint_enable_notification_gestures" />

	<!-- The floating launcher (LauncherService). Switching it on asks for the
	     draw-over-other-apps permission; the two settings below it are the
	     gesture's reach-versus-accidental-triggering trade-off. The zone's
	     entries are populated in code, since what "start" and "end" mean depends
	     on which edge the launcher is docked on. -->
	<SwitchPreferenceCompat
		android:key="launcherservice_enabled"
		android:title="@string/option_launcherservice_enabled"
		android:summary="@string/hint_launcherservice_enabled"
		android:defaultValue="false" />

	<ListPreference
		android:key="launcherservice_zone"
		android:dependency="launcherservice_enabled"
		android:title="@string/option_launcherservice_zone"
		android:dialogTitle="@string/option_launcherservice_zone"
		android:entryValues="@array/launcherservice_zone_values"
		android:defaultValue="full" />

	<ListPreference
		android:key="launcherservice_sensitivity"
		android:dependency="launcherservice_enabled"
		android:title="@string/option_launcherservice_sensitivity"
		android:dialogTitle="@string/option_launcherservice_sensitivity"
		android:entries="@array/launcherservice_sensitivity_entries"
		android:entryValues="@array/launcherservice_sensitivity_values"
		android:defaultValue="medium" />

</PreferenceScreen>

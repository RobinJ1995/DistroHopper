package be.robinj.ubuntu;

import android.content.Context;

import com.snappydb.DB;
import com.snappydb.DBFactory;
import com.snappydb.SnappydbException;

/**
 * Created by robin on 28/06/15.
 */
public class Preference
{
	/*public static boolean getBoolean (Context context, String name, boolean value) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		if (db.exists ((name)))
			value = db.getBoolean (name);
		db.close ();

		return value;
	}

	public static short getShort (Context context, String name, short value) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		if (db.exists ((name)))
			value = db.getShort (name);
		db.close ();

		return value;
	}

	public static int getInt (Context context, String name, int value) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		if (db.exists ((name)))
			value = db.getInt (name);
		db.close ();

		return value;
	}

	public static long getLong (Context context, String name, long value) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		if (db.exists ((name)))
			value = db.getInt (name);
		db.close ();

		return value;
	}

	public static double getDouble (Context context, String name, double value) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		if (db.exists ((name)))
			value = db.getDouble (name);
		db.close ();

		return value;
	}

	public static float getFloat (Context context, String name, float value) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		if (db.exists ((name)))
			value = db.getFloat (name);
		db.close ();

		return value;
	}

	public static String getString (Context context, String name, String value) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		if (db.exists ((name)))
			value = db.get (name);
		db.close ();

		return value;
	}

	public static boolean exists (Context context, String name) throws SnappydbException
	{
		DB db = DBFactory.open (context);
		boolean exists = db.exists (name);
		db.close ();

		return exists;
	}*/
}
